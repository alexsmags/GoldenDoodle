import { renderComponent } from "@/app/__tests__/test-utils";
import ShuttleSchedule from "../ShuttleSchedule";

test('ShuttleSchedule component renders correctly', () => {
    renderComponent(<ShuttleSchedule />);
});