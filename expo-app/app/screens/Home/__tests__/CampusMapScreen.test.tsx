import { renderComponent } from "@/app/__tests__/test-utils";
import CampusMapScreen from "../CampusMapScreen";

test('CampusMapScreen component renders correctly', () => {
    renderComponent(<CampusMapScreen />);
});